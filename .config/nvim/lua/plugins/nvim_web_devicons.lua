-- https://github.com/nvim-tree/nvim-web-devicons
return {
    "nvim-tree/nvim-web-devicons",
    -- use mini.icons instead because they are faster
    enabled = false,
    lazy = true,
    opts = {},
}
